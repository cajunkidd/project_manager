DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
PORT=4000

# Comma-separated list of emails that should always be admins.
# Anyone in this list is auto-promoted to admin on server start, and any
# new registration with one of these emails is created as an admin.
BOOTSTRAP_ADMIN_EMAILS="kyleneely27@gmail.com"
